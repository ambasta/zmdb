import { coreValue } from '@fixture/core';
import { runtimeValue } from 'fixture-runtime';
import { toolValue } from 'fixture-tool';

export const appValue = `${coreValue}:${runtimeValue}:${toolValue}`;
