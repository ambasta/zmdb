name: Publish @zmdb packages to npm

# Publishes conventional dist (.js + .d.ts) to npm using TRUSTED PUBLISHING
# (OIDC) — no NPM_TOKEN. Requires: (1) a Trusted Publisher configured for each
# @zmdb package on npmjs.com pointing at this repo + workflow file `publish.yml`,
# (2) npm CLI >= 11.5.1 (installed below). See PUBLISHING.md.
#
# Trigger: manual dispatch (dry-run by default) or a v* tag.
on:
  workflow_dispatch:
    inputs:
      dry_run:
        description: 'Dry run (build + pack, do not publish)'
        type: boolean
        default: true
  push:
    tags:
      - 'v*'

permissions:
  id-token: write # REQUIRED for OIDC trusted publishing
  contents: read

jobs:
  publish:
    runs-on: ubuntu-latest # OIDC trusted publishing requires GitHub-hosted runners
    steps:
      - uses: actions/checkout@v4

      - name: Enable Corepack (yarn 4)
        run: corepack enable

      - uses: actions/setup-node@v4
        with:
          node-version: '22'
          registry-url: 'https://registry.npmjs.org'

      - name: Upgrade npm to a trusted-publishing-capable version (>= 11.5.1)
        run: npm install -g npm@latest && npm --version

      - name: Install (yarn, immutable)
        run: yarn install --immutable

      - name: Test
        run: yarn vitest run

      - name: Build dist for every package (dependency order)
        run: |
          set -e
          for pkg in schema-core query-compiler aot-validator repository; do
            echo "=== build @zmdb/$pkg ==="
            yarn workspace "@zmdb/$pkg" build
          done

      - name: Repoint package.json → dist (publish manifest)
        run: node .github/scripts/repoint-dist.mjs

      - name: Publish via OIDC (or dry-run) each package
        env:
          DRY: ${{ github.event.inputs.dry_run }}
          # Bypass the root "packageManager: yarn" enforcement so the npm CLI runs.
          COREPACK_ENABLE_PROJECT_SPEC: '0'
        run: |
          set -e
          # Dependency order so dependents resolve on the registry. No token:
          # npm detects the GitHub OIDC environment and authenticates itself.
          for pkg in schema-core query-compiler aot-validator repository; do
            echo "=== packages/$pkg ==="
            cd "packages/$pkg"
            if [ "$DRY" = "false" ]; then
              npm publish --access public
            else
              npm pack --dry-run
            fi
            cd - >/dev/null
          done
