// zmdb/app/commands — curated command-application facade.
export { Command, createCommandApp } from '@zmdb/app/commands';
export type { CommandApp, CommandClass, CommandDef } from '@zmdb/app/commands';
