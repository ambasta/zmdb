// zmdb/web/routing — curated HTTP routing facade.
export { Controller, Delete, Get, Patch, Post, Public, Put, getRoutes, isPublic } from '@zmdb/web/routing';
export type { HttpMethod, ResolvedRoute, RouteDefinition } from '@zmdb/web/routing';
