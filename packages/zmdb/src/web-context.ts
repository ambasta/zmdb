// zmdb/web/context — curated request-context facade.
export { compilePattern, countSegments, extractParams, matchCompiled } from '@zmdb/web/context';
export type { CompiledPattern, Ctx, HandlerFor, PathParams, QueryValues } from '@zmdb/web/context';
