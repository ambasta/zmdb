// zmdb/app/events — curated application-events facade.
export { OnEvent, createEvents, getEventHandlers } from '@zmdb/app/events';
export type { EmitReport, EventFailure, EventMap, Events, EventsOptions, ResolvedEventHandler } from '@zmdb/app/events';
