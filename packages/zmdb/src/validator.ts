// zmdb/validator — explicit named re-exports.
export {
  ValidationError,
  getCachedRegExp,
  getEnumSet,
  getRegExp,
  tags,
  validate,
  validatePatternComplexity,
} from '@zmdb/aot-validator';
export type { Rule } from '@zmdb/aot-validator';
