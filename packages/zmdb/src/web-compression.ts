// zmdb/web/compression — curated HTTP compression facade.
export { compress, compressionInterceptor } from '@zmdb/web/compression';
export type { CompressionOptions, ContentCoding } from '@zmdb/web/compression';
