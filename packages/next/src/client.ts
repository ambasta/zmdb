'use client';

export { createZmdbReact as createZmdbNextClient } from '@zmdb/react';
export type {
  MutationRunner,
  MutationState,
  QueryLoader,
  QueryState,
  ZmdbClientProviderProps,
  ZmdbReactBindings,
} from '@zmdb/react';
