{
  "name": "zmdb-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "benchmarks"
  ],
  "type": "module",
  "scripts": {
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "postinstall": "sh scripts/setup-hooks.sh",
    "prepare": "sh scripts/setup-hooks.sh",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p packages/schema-core/tsconfig.json && tsc --noEmit -p packages/query-compiler/tsconfig.json && tsc --noEmit -p packages/aot-validator/tsconfig.json && tsc --noEmit -p packages/repository/tsconfig.json && tsc --noEmit -p benchmarks/tsconfig.json"
  },
  "devDependencies": {
    "esbuild": "^0.27.7",
    "tsup": "8.5.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=26"
  },
  "packageManager": "yarn@4.18.0"
}
