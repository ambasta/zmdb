{
  "name": "zmdb-monorepo",
  "private": true,
  "workspaces": [
    "packages/*",
    "benchmarks"
  ],
  "type": "module",
  "scripts": {
    "build": "yarn workspaces foreach -A -t run build && node .github/scripts/emit-dts.mjs",
    "build:docs": "node docs-site/build.mjs",
    "fmt": "oxfmt",
    "fmt:check": "oxfmt --check",
    "guardrail": "node --experimental-strip-types benchmarks/src/cli.ts",
    "hooks:install": "sh scripts/setup-hooks.sh",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "yarn build && node scripts/typecheck.mjs",
    "validate:spec": "node .github/scripts/validate-specs.mjs",
    "verify-exports": "node .github/scripts/verify-exports.mjs"
  },
  "devDependencies": {
    "@types/node": "^26.4.0",
    "esbuild": "^0.28.2",
    "oxfmt": "^0.65.0",
    "oxlint": "^1.80.0",
    "tsup": "8.5.1",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^4.1.11"
  },
  "engines": {
    "node": ">=26"
  },
  "packageManager": "yarn@4.18.0"
}
