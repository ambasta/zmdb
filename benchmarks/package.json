{
  "name": "@zmdb/benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "guardrail": "node --experimental-strip-types src/cli.ts",
    "test": "vitest run"
  },
  "dependencies": {
    "@zmdb/aot-validator": "workspace:*",
    "@zmdb/query-compiler": "workspace:*",
    "kysely": "^0.29.5"
  },
  "engines": {
    "node": ">=26"
  }
}
