{
  "name": "@zmdb/benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "dependencies": {
    "@zmdb/aot-validator": "workspace:*",
    "@zmdb/query-compiler": "workspace:*"
  },
  "devDependencies": {
    "kysely": "^0.29.0"
  },
  "scripts": {
    "test": "vitest run"
  }
}
