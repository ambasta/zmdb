{
  "name": "@zmdb/benchmarks",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=26"
  },
  "dependencies": {
    "@zmdb/aot-validator": "workspace:*"
  },
  "scripts": {
    "test": "vitest run"
  }
}
